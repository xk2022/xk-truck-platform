# 20260110 後端程式碼優化建議

**文件建立日期：** 2026-01-10  
**專案名稱：** xk-truck-platform  
**掃描範圍：** 完整專案後端程式碼

---

## 📋 目錄

- [一、架構層面優化](#一架構層面優化)
- [二、程式碼品質優化](#二程式碼品質優化)
- [三、效能優化](#三效能優化)
- [四、安全性優化](#四安全性優化)
- [五、可維護性優化](#五可維護性優化)
- [六、測試覆蓋率](#六測試覆蓋率)
- [七、優先級建議](#七優先級建議)

---

## 一、架構層面優化

### 1.1 Checkstyle 配置啟用 ⚠️ 中優先級

**現況：**
- `checkstyle.xml` 檔案存在但內容全部被註解
- 未在 CI/CD 流程中啟用程式碼風格檢查

**建議：**
1. 啟用 `checkstyle.xml` 配置，建議先採用寬鬆規則（不阻斷建置）
2. 在 Maven `verify` phase 加入 Checkstyle 檢查
3. 逐步收緊規則，最終目標是阻斷不符合規範的程式碼

**實作步驟：**
```xml
<!-- pom.xml -->
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-checkstyle-plugin</artifactId>
    <executions>
        <execution>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
        </execution>
    </executions>
</plugin>
```

---

### 1.2 資料庫遷移工具整合 🔴 高優先級

**現況：**
- 使用 `hibernate.ddl-auto: update` 進行自動建表
- 開發環境尚可接受，但正式環境不適合

**建議：**
引入 **Flyway** 或 **Liquibase** 進行資料庫版本管理

**優點：**
- 版本化資料庫結構變更
- 可追蹤資料庫變更歷史
- 支援多環境（dev / test / prod）遷移
- 可執行資料遷移腳本（Data Migration）

**實作建議（Flyway）：**
```xml
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-core</artifactId>
</dependency>
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-mysql</artifactId>
</dependency>
```

```yaml
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
```

---

### 1.3 異常處理統一化 ⚠️ 中優先級

**現況：**
- 已有 `GlobalExceptionHandler` 與 `TruckExceptionHandler`
- 部分 Service 仍直接拋出 `IllegalArgumentException`、`EntityNotFoundException`

**建議：**
1. 統一使用自定義例外類別：
   - `BusinessException`：業務邏輯例外
   - `ResourceNotFoundException`：資源不存在
   - 新增 `ValidationException`：驗證失敗
2. 所有 Service 層統一拋出自定義例外
3. 在 Controller 層或 GlobalExceptionHandler 統一轉換為 `ApiResult`

**範例：**
```java
// ❌ 不建議
throw new IllegalArgumentException("電話已存在: " + phone);

// ✅ 建議
throw new BusinessException("DRIVER_PHONE_EXISTS", "電話已存在: " + phone);
```

---

### 1.4 分頁查詢規範化 ⚠️ 中優先級

**現況：**
- 部分模組已有分頁實作，但未統一規範
- 缺少統一的 Query DTO 與 Specification 規範

**建議：**
1. 建立統一的 `PageQuery` 基礎類別（包含 page、size、sort）
2. 建立統一的 `Specification` 工具類別（避免 N+1 問題）
3. 所有列表查詢 API 統一使用後端分頁（避免前端分頁）

**參考現有實作：**
- `com.xk.base.web.dto.query.PageQuery`（已存在）
- 建議擴充為支援動態查詢條件的 `DynamicSpecification`

---

### 1.5 API 版本控制 🔵 低優先級

**現況：**
- API 路徑未包含版本號
- 未來可能需要進行 API 版本管理

**建議：**
1. 考慮引入 API 版本控制（例如：`/api/v1/users`）
2. 使用 Spring 的 `@RequestMapping` 或自訂 Filter 實現版本路由
3. 文件化版本策略（何時建立新版本、舊版本保留期限）

---

## 二、程式碼品質優化

### 2.1 Service 層 Transaction 管理 ⚠️ 中優先級

**現況：**
- 部分 Service 類別使用 `@Transactional(readOnly = true)` 在類別層級
- 寫入方法個別標記 `@Transactional`，但讀取方法可能遺漏 `readOnly`

**建議：**
1. 統一 Service 層級 Transaction 管理策略：
   ```java
   @Service
   @Transactional(readOnly = true)  // 預設唯讀
   public class XxxService {
       @Transactional  // 寫入方法明確標記
       public void create(...) { ... }
   }
   ```
2. 避免在 Controller 層使用 `@Transactional`
3. 針對複雜業務流程，考慮使用 `@Transactional(propagation = Propagation.REQUIRES_NEW)` 進行事務隔離

---

### 2.2 Repository 查詢優化 🔴 高優先級

**現況：**
- 部分 Repository 使用 `findAll()` 後在記憶體中過濾
- 可能存在 N+1 查詢問題（特別是關聯查詢）

**建議：**
1. **使用 Specification 動態查詢**：避免先查全部再過濾
   ```java
   // ❌ 不建議
   List<User> users = userRepository.findAll();
   return users.stream().filter(u -> u.getName().contains(keyword))...
   
   // ✅ 建議
   Specification<User> spec = (root, query, cb) -> 
       cb.like(root.get("name"), "%" + keyword + "%");
   return userRepository.findAll(spec, pageable);
   ```

2. **使用 EntityGraph 避免 N+1**：
   ```java
   @EntityGraph(attributePaths = {"userRoles", "userRoles.role"})
   Optional<UpmsUser> findByUsername(String username);
   ```

3. **使用 DTO Projection**：只查詢需要的欄位
   ```java
   @Query("SELECT new com.xk.truck.upms.dto.UserListResp(u.uuid, u.username, u.enabled) FROM UpmsUser u")
   Page<UserListResp> findAllProjected(Pageable pageable);
   ```

---

### 2.3 DTO 驗證加強 ⚠️ 中優先級

**現況：**
- 部分 DTO 使用 Jakarta Validation 註解，但未完整
- Controller 層可能缺少 `@Valid` 或 `@Validated`

**建議：**
1. 所有 Request DTO 加入完整的 Validation 註解：
   ```java
   public class DriverCreateReq {
       @NotBlank(message = "姓名不能為空")
       @Size(max = 50, message = "姓名長度不能超過 50 字元")
       private String name;
       
       @NotBlank(message = "電話不能為空")
       @Pattern(regexp = "^09\\d{8}$", message = "電話格式不正確")
       private String phone;
   }
   ```

2. Controller 方法參數加入 `@Valid`：
   ```java
   @PostMapping
   public ApiResult<DriverResp> create(@Valid @RequestBody DriverCreateReq req) {
       ...
   }
   ```

3. 建立自訂 Validator 處理複雜驗證邏輯（例如：電話唯一性）

---

### 2.4 日誌規範化 ⚠️ 中優先級

**現況：**
- 部分 Service 使用 `@Slf4j`，但日誌層級不一致
- 缺少統一的日誌格式與關鍵業務操作記錄

**建議：**
1. 統一日誌層級：
   - `ERROR`：系統錯誤、例外
   - `WARN`：警告、可恢復的錯誤
   - `INFO`：關鍵業務操作（建立、更新、刪除）
   - `DEBUG`：開發期除錯資訊
   - `TRACE`：詳細追蹤資訊

2. 關鍵操作記錄：
   ```java
   log.info("[FMS][Vehicle] 建立車輛: plateNo={}, type={}, operator={}", 
            req.getPlateNo(), req.getType(), getCurrentUser());
   ```

3. 敏感資訊避免記錄：密碼、Token、個人隱私資料

4. 使用結構化日誌（建議未來引入 Logback JSON Encoder）

---

### 2.5 常數管理規範化 ⚠️ 中優先級

**現況：**
- 部分常數直接寫死在程式碼中（例如：錯誤代碼、狀態值）

**建議：**
1. 建立常數類別統一管理：
   ```java
   public class ErrorCodes {
       public static final String DRIVER_PHONE_EXISTS = "DRIVER_PHONE_EXISTS";
       public static final String VEHICLE_PLATE_EXISTS = "VEHICLE_PLATE_EXISTS";
   }
   ```

2. 或使用 Enum 管理（更類型安全）：
   ```java
   public enum ErrorCode {
       DRIVER_PHONE_EXISTS("DRIVER_PHONE_EXISTS", "電話已存在"),
       VEHICLE_PLATE_EXISTS("VEHICLE_PHONE_EXISTS", "車牌已存在");
   }
   ```

---

## 三、效能優化

### 3.1 資料庫連線池優化 🔴 高優先級

**現況：**
- 使用預設 HikariCP 設定（maximum-pool-size: 10）

**建議：**
根據實際負載調整連線池參數：
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20        # 根據併發量調整
      minimum-idle: 5              # 保持最小連線數
      connection-timeout: 30000    # 連線超時
      idle-timeout: 600000         # 空閒連線超時（10分鐘）
      max-lifetime: 1800000        # 連線最大生命週期（30分鐘）
      leak-detection-threshold: 60000  # 連線洩漏檢測（60秒）
```

---

### 3.2 資料庫索引優化 🔴 高優先級

**現況：**
- 部分查詢欄位可能缺少索引（例如：電話、車牌、使用者名稱）

**建議：**
1. 為所有唯一約束欄位建立索引（已存在 unique constraint，但需確認索引存在）
2. 為常用查詢條件建立複合索引：
   ```sql
   -- 範例：使用者查詢（username, enabled）
   CREATE INDEX idx_upms_user_username_enabled ON upms_user(username, enabled);
   
   -- 範例：車輛查詢（status, type）
   CREATE INDEX idx_fms_vehicle_status_type ON fms_vehicle(status, type);
   ```
3. 定期分析慢查詢日誌（MySQL `slow_query_log`）

---

### 3.3 批次操作優化 ⚠️ 中優先級

**現況：**
- 部分批次操作可能使用逐筆 insert/update

**建議：**
1. 使用 JPA `saveAll()` 進行批次 insert（需配合 `spring.jpa.properties.hibernate.jdbc.batch_size`）
2. 大量資料更新考慮使用 `@Modifying` + `@Query` 進行批次更新
3. 避免在迴圈中進行資料庫操作

**範例：**
```java
// ❌ 不建議
for (User user : users) {
    userRepository.save(user);  // N 次資料庫操作
}

// ✅ 建議
userRepository.saveAll(users);  // 批次操作
```

```yaml
spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 50  # 批次大小
        order_inserts: true
        order_updates: true
```

---

### 3.4 快取機制引入 🔵 低優先級

**現況：**
- 未使用快取機制

**建議：**
1. 針對讀多寫少的資料引入快取（例如：字典資料、系統參數、權限資料）
2. 使用 Spring Cache + Redis：
   ```java
   @Cacheable(value = "dictItems", key = "#categoryCode")
   public List<AdmDictItem> findByCategoryCode(String categoryCode) {
       ...
   }
   ```
3. 注意快取一致性問題（更新時需清除快取）

---

## 四、安全性優化

### 4.1 JWT Secret 管理 🔴 高優先級

**現況：**
- JWT Secret 寫在 `application.yml` 中（雖然有環境變數支援，但需確保正式環境使用環境變數）

**建議：**
1. **絕對不要**將 Secret 提交到版本控制系統
2. 使用環境變數或 Secrets Manager（例如：AWS Secrets Manager、HashiCorp Vault）
3. 開發環境使用 `.env` 檔案（加入 `.gitignore`）
4. 定期輪換 JWT Secret

---

### 4.2 密碼加密加強 ⚠️ 中優先級

**現況：**
- 使用 BCrypt 進行密碼加密（已正確）

**建議：**
1. 確認 BCrypt 強度設定（建議 strength >= 10）
2. 考慮引入密碼複雜度規則（在建立/更新使用者時驗證）
3. 實作密碼重設流程（忘記密碼功能）

---

### 4.3 SQL Injection 防護 ⚠️ 中優先級

**現況：**
- 使用 JPA / Hibernate，已能防護大部分 SQL Injection
- 但需注意原生 SQL 查詢

**建議：**
1. 避免使用字串拼接原生 SQL
2. 使用參數化查詢：
   ```java
   // ❌ 不建議
   @Query("SELECT u FROM User u WHERE u.name = '" + name + "'")
   
   // ✅ 建議
   @Query("SELECT u FROM User u WHERE u.name = :name")
   List<User> findByName(@Param("name") String name);
   ```

---

### 4.4 API 速率限制 🔵 低優先級

**建議：**
1. 引入 Spring Security 的 Rate Limiting
2. 或使用 Redis 實作自訂 Rate Limiter
3. 針對登入 API 特別加強（防止暴力破解）

---

## 五、可維護性優化

### 5.1 單元測試覆蓋率 🔴 高優先級

**現況：**
- 測試檔案較少，覆蓋率不足

**建議：**
1. 為所有 Service 層撰寫單元測試
2. 使用 Mockito 模擬 Repository 依賴
3. 使用 JUnit 5 與 AssertJ 提升測試可讀性
4. 目標覆蓋率：Service 層 >= 80%

**範例：**
```java
@ExtendWith(MockitoExtension.class)
class DriverServiceTest {
    @Mock
    private DriverRepository repository;
    
    @InjectMocks
    private DriverService service;
    
    @Test
    void shouldCreateDriverSuccessfully() {
        // Given
        DriverCreateReq req = new DriverCreateReq("王小明", "0912000123", ...);
        Driver saved = Driver.builder()...build();
        when(repository.findByPhone(req.getPhone())).thenReturn(Optional.empty());
        when(repository.save(any())).thenReturn(saved);
        
        // When
        DriverResp result = service.create(req);
        
        // Then
        assertThat(result.getName()).isEqualTo("王小明");
        verify(repository).save(any());
    }
}
```

---

### 5.2 整合測試 🔴 高優先級

**建議：**
1. 為關鍵業務流程撰寫整合測試（例如：建立訂單流程）
2. 使用 `@SpringBootTest` + Testcontainers（MySQL）
3. 或使用 H2 記憶體資料庫進行快速測試

**現有範例參考：**
- `AuthControllerIT.java`

---

### 5.3 API 文件完善 ⚠️ 中優先級

**現況：**
- 已使用 Swagger / OpenAPI，但部分 API 缺少詳細說明

**建議：**
1. 為所有 Controller 方法加入 `@Operation` 註解
2. 為所有 DTO 欄位加入 `@Schema` 註解（包含 description、example）
3. 為錯誤回應加入 `@ApiResponse` 說明
4. 加入 API 使用範例

---

### 5.4 程式碼註解與文件 ⚠️ 中優先級

**現況：**
- 部分複雜業務邏輯缺少註解
- 部分類別已有良好的 JavaDoc，但未統一

**建議：**
1. 為所有公開方法加入 JavaDoc（至少包含方法說明、參數說明、回傳值說明）
2. 複雜業務邏輯加入行內註解說明
3. 使用 `@see` 引用相關文件或方法
4. 統一註解風格（建議使用現有的多行分隔風格）

---

### 5.5 錯誤訊息國際化 🔵 低優先級

**建議：**
1. 引入 Spring MessageSource 進行多語系支援
2. 將所有錯誤訊息抽取到 `messages.properties` / `messages_zh_TW.properties`
3. 使用 `MessageSource` 在 Exception Handler 中取得對應語系訊息

---

## 六、測試覆蓋率

### 6.1 現況分析

根據掃描結果，專案測試檔案較少：
- 僅有少數整合測試（`AuthControllerIT.java`）
- Service 層缺少單元測試
- Repository 層缺少測試

### 6.2 建議測試優先順序

1. **第一優先：關鍵業務流程**
   - 訂單建立流程（CreateTomOrderService）
   - 使用者登入流程（AuthService）
   - 車輛/司機建立流程

2. **第二優先：Service 層**
   - 所有 Service 的 CRUD 操作
   - 業務規則驗證（例如：唯一性檢查）

3. **第三優先：Controller 層**
   - API 端點測試
   - 參數驗證測試
   - 權限檢查測試

---

## 七、優先級建議

### 🔴 高優先級（立即處理）

1. **資料庫遷移工具整合（Flyway）**
   - 影響：正式環境部署安全性
   - 預估工時：2-3 天

2. **Repository 查詢優化（避免 N+1、使用 Specification）**
   - 影響：系統效能
   - 預估工時：3-5 天

3. **單元測試與整合測試**
   - 影響：程式碼品質與穩定性
   - 預估工時：持續進行

4. **資料庫索引優化**
   - 影響：查詢效能
   - 預估工時：1-2 天

### ⚠️ 中優先級（近期處理）

1. **Checkstyle 配置啟用**
2. **異常處理統一化**
3. **Service 層 Transaction 管理優化**
4. **DTO 驗證加強**
5. **日誌規範化**
6. **API 文件完善**

### 🔵 低優先級（長期規劃）

1. **API 版本控制**
2. **快取機制引入**
3. **API 速率限制**
4. **錯誤訊息國際化**

---

## 📝 總結

本專案整體架構良好，採用 DDD 與 Clean Architecture 設計理念，模組劃分清晰。主要優化方向集中在：

1. **穩定性提升**：測試覆蓋率、異常處理統一化
2. **效能優化**：查詢優化、索引優化、批次操作
3. **可維護性**：程式碼規範、文件完善
4. **安全性**：Secret 管理、SQL Injection 防護

建議按照優先級逐步實作，並在每次優化後進行測試驗證。

---

**文件維護：** 建議每季度更新此文件，追蹤優化進度與新發現的問題。
