很好，這份內容我已幫你 **完整彙整、去重、補齊缺漏、統一技術版本與模組定義**，整理成一份：

> ✅ **可直接作為正式專案根目錄 `README.md` 使用版**
> ✅ 同時滿足：
>
> * 新 GPT 快速匯入系統背景
> * 新工程師 Onboarding
> * 團隊內技術交接
> * MVP 交付說明

以下是「**最終完整版・技術交付等級 README.md**」⬇️
你可以 **整份直接複製使用**。

---

# 🚚 XK Truck Platform（xk-truck-platform）

本專案為 **模組化單體系統（Modular Monolith）**，基於
Spring Boot + Java + MySQL + React 建構，
專注於 **物流訂單管理 × 車輛調派 × 權限控管（MVP 平台）**。

---

## ✅ 一、系統目標與定位

本系統為企業內部使用之 **物流管理中台系統**，核心目標：

* ✅ 統一使用者 / 角色 / 權限管理（UPMS）
* ✅ 系統字典與參數設定（ADM）
* ✅ 車輛與司機管理（FMS）
* ✅ 客戶訂單、指派與狀態流轉（ORDER / TOM）
* ✅ 前後端拆分、模組化單體架構
* ✅ 作為後續 ERP / 運輸管理系統（TMS）之基礎核心

---

## ✅ 二、系統整體後端架構（模組化單體）

```
xk-truck-platform
│
├─ pom.xml                     # Parent POM（版本統一）
│
├─ xk-base                     ✅ 共用核心模組（Base）
│  ├─ web                      → ApiResult、GlobalExceptionHandler
│  ├─ security                 → JWT、JwtFilter、SecurityConfig
│  ├─ config                   → Swagger、Jackson、TimeZone
│  └─ util                     → XkBeanUtils
│
├─ xk-upms                     ✅ 使用者與權限管理
│  ├─ controller
│  ├─ domain
│  ├─ repository
│  ├─ dto
│
├─ xk-adm                      ✅ 系統字典與參數管理
│
├─ xk-fms                      ✅ 車輛 / 司機管理
│
├─ xk-order                    ✅ 訂單 / 派遣 / 狀態機
│
└─ xk-application              ✅ Spring Boot 啟動模組
```

---

## ✅ 三、各模組職責說明

| 模組               | 說明                                |
| ---------------- | --------------------------------- |
| `xk-base`        | JWT 安全、API 統一回傳、例外處理、Swagger、共用工具 |
| `xk-upms`        | 使用者、角色、權限、登入                      |
| `xk-adm`         | 系統字典檔、下拉選單、參數設定                   |
| `xk-fms`         | 車輛、司機、派遣狀態                        |
| `xk-order`       | 訂單建立、指派、狀態流轉                      |
| `xk-application` | 系統啟動入口                            |

---

## ✅ 四、後端技術棧（統一規格）

| 類別  | 規格                            |
| --- | ----------------------------- |
| 語言  | Java 21                       |
| 框架  | Spring Boot 3.5.x             |
| ORM | Spring Data JPA / Hibernate 6 |
| 安全  | Spring Security 6 + JWT       |
| 資料庫 | MySQL 8.x                     |
| 文件  | Swagger / OpenAPI             |
| 建置  | Maven 3.9+                    |
| 容器  | Docker Compose（部署用）           |

---

## ✅ 五、JWT 安全架構（後端）

### 🔐 JWT 登入流程

1. `POST /auth/login` → 傳入帳號密碼
2. 驗證成功 → 回傳 JWT Token
3. 所有 `/api/**` 必須夾帶：

```
Authorization: Bearer <token>
```

4. 由 `JwtAuthFilter` 驗證並注入 `SecurityContextHolder`

---

### ✅ 密碼編碼規則

* 採用 `DelegatingPasswordEncoder`
* DB 密碼格式必須為：

```
{bcrypt}$2a$10$xxxxxxxxxxxxxxxx
```

---

## ✅ 六、API 統一回傳格式

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": "2025-01-01T00:00:00"
}
```

### ✅ 分頁格式

```json
{
  "content": [],
  "totalElements": 0,
  "totalPages": 0,
  "size": 10,
  "number": 0,
  "first": true,
  "last": true,
  "empty": true
}
```

---

## ✅ 七、目前已完成功能模組

| 模組    | 功能狀態                |
| ----- | ------------------- |
| UPMS  | ✅ 使用者 CRUD、角色、權限、登入 |
| ADM   | ✅ 字典檔查詢             |
| FMS   | ✅ 車輛、司機 API         |
| ORDER | ✅ 訂單基礎結構            |

---

## ✅ 八、前端架構（React）

```
src/app/pages
│
├─ upms/user
│  ├─ Overview.tsx
│  ├─ List.tsx
│  ├─ FormModal.tsx   ✅ 共用 Create + Edit
│  ├─ Query.ts
│  └─ UserModel.ts
│
├─ adm/dictionary
├─ fms/vehicle
├─ fms/driver
└─ order
```

---

## ✅ 九、前端設計原則（你目前的規範）

* ✅ 所有 CRUD 統一使用 `FormModal`
* ✅ 由 `uuid` 判斷 Create / Edit
* ✅ 不拆 CreateModal / EditModal
* ✅ Overview → List → Modal 統一流程
* ✅ List 重新掛載使用 `setListKey(prev => prev + 1)`

---

## ✅ 十、Sidebar 路由對應表

| 系統  | 路由                          |
| --- | --------------------------- |
| 使用者 | `/upms/user/overview`       |
| 角色  | `/upms/role/overview`       |
| 權限  | `/upms/permission/overview` |
| 字典  | `/adm/dictionary/overview`  |
| 車輛  | `/fms/vehicle/list`         |
| 司機  | `/fms/driver/list`          |
| 訂單  | `/order/list`               |

---

## ✅ 十一、資料庫訂單表（核心範例）

```sql
CREATE TABLE t_order (
  uuid           varchar(36) PRIMARY KEY,
  order_no       varchar(32) UNIQUE NOT NULL,
  customer_name  varchar(64) NOT NULL,
  pickup_addr    varchar(255) NOT NULL,
  delivery_addr  varchar(255) NOT NULL,
  status         enum('CREATED','ACCEPTED','DISPATCHED','PICKED_UP','DELIVERED','COMPLETED','CANCELED') NOT NULL,
  created_time   datetime NOT NULL,
  updated_time   datetime NOT NULL
);
```

---

## ✅ 十二、快速啟動（新 GPT / 新工程師用）

### 1️⃣ 建立資料庫

```sql
CREATE DATABASE xk_truck DEFAULT CHARSET utf8mb4;
```

### 2️⃣ 設定 `application.yml`

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xk_truck
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: xkSecretKey
  expiration: 86400000
```

### 3️⃣ 啟動

```bash
mvn clean install
mvn spring-boot:run
```

### 4️⃣ Swagger

```
http://localhost:8080/swagger-ui/index.html
```

---

## ✅ 十三、常見錯誤整理（你實際遇過）

| 問題                                          | 解法                            |
| ------------------------------------------- | ----------------------------- |
| `Bad credentials`                           | 密碼缺少 `{bcrypt}`               |
| `AuthenticationManager not found`           | 補上 AuthenticationManager Bean |
| `daoAuthenticationProvider already defined` | Base 與 Truck 重複註冊             |
| 登入成功後 403                                   | `/auth/login` 未 permitAll     |

---

## ✅ 十四、Roadmap（v1～v4）

| 版本 | 內容        |
| -- | --------- |
| v1 | 基礎 CRUD   |
| v2 | 詳情、指派     |
| v3 | 匯入匯出、報表   |
| v4 | 2FA、IP 控管 |

---

## ✅ 十五、團隊

* Backend / Architect：Louis（祿元）
* PM：Lisa
* Backend：Hank
* Frontend：Tim

---

## ✅ 十六、授權

```
Copyright (c) 2025 XploreKaleidoscope
All rights reserved.
```

---

## ✅ 十七、新 GPT 專用快速開場提示（你最重要的一段）

```
這個專案是基於 Java 21 / Spring Boot 3.5 的多模組架構，
root 為 xk-truck-platform，
子模組包含 xk-base（安全、API 統一、JWT）、
xk-upms（使用者權限）、
xk-adm（字典）、
xk-fms（車輛司機）、
xk-order（訂單與指派）。
目前系統已有 JWT 登入、UPMS、ORDER CRUD，
請協助我繼續開發下一個模組。
```

---

# ✅ 我現在可以再幫你補這 4 份文件（實戰用）

你可以直接選一個：

1️⃣ 《UPMS / FMS / ORDER 完整 API 對照表》
2️⃣ 《資料庫 ERD 設計圖＋關聯說明》
3️⃣ 《客戶用技術白皮書（商業版 README）》
4️⃣ 《新工程師 7 天 Onboarding 教學手冊》

---

👉 你可以直接回我一句：
**「我要補第 X 份」**
我就幫你產出正式交付版本。
