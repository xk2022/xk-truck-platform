# 業務邏輯定義文件

**文件建立日期：** 2026-01-10  
**專案名稱：** xk-truck-platform  
**版本：** 1.0.0

---

## 📋 目錄

- [一、系統概述](#一系統概述)
- [二、核心業務模組](#二核心業務模組)
- [三、領域模型定義](#三領域模型定義)
- [四、業務流程定義](#四業務流程定義)
- [五、狀態機定義](#五狀態機定義)
- [六、業務規則](#六業務規則)
- [七、資料字典](#七資料字典)

---

## 一、系統概述

### 1.1 系統定位

**XK Truck Platform** 是一個企業內部使用的**物流管理中台系統**，主要提供以下核心功能：

1. **使用者與權限管理（UPMS）**：系統帳號、角色、權限的統一管理
2. **系統字典與參數管理（ADM）**：系統配置資料與字典資料管理
3. **車隊管理（FMS）**：車輛、司機、派工任務的管理
4. **運輸訂單管理（TOM）**：客戶訂單的建立、受理、指派流程管理

### 1.2 設計理念

本系統採用 **DDD（Domain-Driven Design）** 設計理念，並遵循 **Clean Architecture** 與 **Hexagonal Architecture** 原則：

- **OrderCore（命運層）**：跨 Domain 的訂單共同語言，只描述訂單高階生命週期
- **TOM（流程層）**：運輸訂單的業務流程管理
- **FMS（資源層）**：車輛與司機等資源管理
- **UPMS（基礎層）**：使用者與權限基礎設施

---

## 二、核心業務模組

### 2.1 UPMS（使用者與權限管理系統）

#### 2.1.1 功能範圍

- **使用者管理**：帳號建立、更新、啟用/停用、刪除、密碼重設
- **角色管理**：角色建立、更新、啟用/停用、刪除
- **權限管理**：權限建立、更新、刪除
- **使用者角色指派**：為使用者指派或移除角色
- **角色權限指派**：為角色指派或移除權限
- **登入認證**：JWT Token 產生與驗證

#### 2.1.2 核心實體

- **UpmsUser**：使用者主實體
  - 欄位：uuid、username（唯一）、password（BCrypt 加密）、enabled、locked、lastLoginTime 等
- **UpmsRole**：角色
  - 欄位：uuid、code（唯一）、name、description、enabled、sortOrder 等
- **UpmsPermission**：權限
  - 欄位：uuid、code（唯一，格式：`{SYSTEM}_{RESOURCE}_{ACTION}`）、systemCode、resourceCode、actionCode、name、description、enabled 等
- **UpmsUserRole**：使用者角色關聯表（多對多）
- **UpmsRolePermission**：角色權限關聯表（多對多）

#### 2.1.3 業務規則

1. **使用者帳號唯一性**：username 必須唯一
2. **角色代碼唯一性**：role.code 必須唯一
3. **權限代碼唯一性**：permission.code 必須唯一（由 systemCode + resourceCode + actionCode 組成）
4. **密碼加密**：使用 BCrypt 進行密碼加密
5. **登入失敗鎖定**：可實作連續登入失敗次數限制（目前未實作）
6. **權限繼承**：使用者擁有的權限 = 其所有角色的權限集合

---

### 2.2 ADM（系統字典與參數管理）

#### 2.2.1 功能範圍

- **字典類別管理**：建立、更新、刪除字典分類
- **字典項目管理**：在分類下建立、更新、刪除字典項目，支援排序
- **系統參數管理**：系統配置參數的 CRUD 操作

#### 2.2.2 核心實體

- **AdmDictCategory**：字典分類
  - 欄位：uuid、code（唯一）、name、enabled、remark
- **AdmDictItem**：字典項目
  - 欄位：uuid、categoryId（外鍵）、code、name、value、sortOrder、enabled、remark
- **SysParam**：系統參數
  - 欄位：uuid、key（唯一）、value、description、enabled

#### 2.2.3 業務規則

1. **字典分類代碼唯一性**：category.code 必須唯一
2. **字典項目唯一性**：同一分類下，item.code 必須唯一
3. **系統參數鍵唯一性**：param.key 必須唯一
4. **字典項目排序**：支援在同一分類內調整項目順序（sortOrder）

---

### 2.3 FMS（車隊管理系統）

#### 2.3.1 功能範圍

- **車輛管理**：車輛建立、更新、狀態管理（可用/使用中/維修/報廢）
- **司機管理**：司機建立、更新、狀態管理（在職/離職）、上線狀態管理
- **車輛司機綁定**：車輛與司機的綁定關係管理（VehicleAssignment）
- **派工任務管理**：建立派工任務、指派車輛與司機、追蹤派工狀態
- **車輛里程記錄**：記錄車輛里程數（VehicleOdometerRecord）
- **車輛維修記錄**：記錄車輛維修與保養資訊（VehicleMaintenanceRecord）

#### 2.3.2 核心實體

- **Vehicle**：車輛
  - 欄位：id（UUID）、plateNo（車牌，唯一）、type（VehicleType）、status（VehicleStatus）、brand、model、capacityTon（載重噸數）、currentDriverId
- **Driver**：司機
  - 欄位：id（UUID）、name、phone（電話，唯一）、licenseType（DriverLicenseType）、status（DriverStatus）、onDuty（是否上線）、currentVehicleId、userId（關聯 UpmsUser）
- **DispatchTask**：派工任務
  - 欄位：id（UUID）、orderId、vehicleId、driverId、status（DispatchStatus）、assignedAt、completedAt
- **VehicleAssignment**：車輛司機綁定記錄
- **VehicleOdometerRecord**：車輛里程記錄
- **VehicleMaintenanceRecord**：車輛維修記錄

#### 2.3.3 業務規則

1. **車牌唯一性**：vehicle.plateNo 必須唯一
2. **司機電話唯一性**：driver.phone 必須唯一
3. **車輛狀態流轉**：
   - AVAILABLE → BUSY（指派任務時）
   - BUSY → AVAILABLE（任務完成時）
   - AVAILABLE → MAINTENANCE（進入維修）
   - MAINTENANCE → AVAILABLE（維修完成）
4. **司機上線狀態**：只有 onDuty = true 的司機才能被指派任務
5. **派工規則**：派工時需檢查車輛狀態為 AVAILABLE、司機狀態為 ACTIVE 且 onDuty = true

---

### 2.4 OrderCore（訂單核心 - 命運層）

#### 2.4.1 功能範圍

- **訂單核心建立**：建立跨 Domain 的訂單核心記錄
- **訂單狀態管理**：訂單高階生命週期狀態管理（OPEN / CANCELLED / CLOSED）

#### 2.4.2 核心實體

- **OrderCore**：訂單核心 Aggregate Root
  - 欄位：orderUuid（UUID，主鍵）、orderNo（訂單編號，唯一）、status（OrderCoreStatus）

#### 2.4.3 業務規則

1. **訂單編號唯一性**：orderNo 必須唯一
2. **狀態流轉規則**：
   - 新建訂單：status = OPEN
   - 取消訂單：status = CANCELLED（終態）
   - 結案訂單：status = CLOSED（終態）
3. **跨 Domain 共用**：OrderCore 作為所有 Domain（TOM、COM、SOM、FIN 等）的共同語言

---

### 2.5 TOM（運輸訂單管理 - 流程層）

#### 2.5.1 功能範圍

- **運輸訂單建立**：建立運輸訂單（與 OrderCore 協同建立）
- **訂單受理**：受理新建訂單，進入可安排階段
- **訂單指派**：為訂單指派車輛與司機
- **訂單取消**：取消訂單（需檢查流程狀態）

#### 2.5.2 核心實體

- **TomOrder**：運輸訂單 Aggregate Root
  - 欄位：orderUuid（與 OrderCore 共享主鍵）、orderNo、orderType（TomOrderType）、tomStatus（TomOrderStatus）、customerUuid、customerName、pickupAddress、deliveryAddress、scheduledAt、customerRefNo、remark

#### 2.5.3 業務規則

1. **與 OrderCore 協同**：建立 TOM 訂單時，需先建立 OrderCore
2. **訂單編號產生**：由 OrderNoGeneratorPort 產生唯一訂單編號（格式可自訂，例如：TOM-YYYYMMDD-XXXX）
3. **狀態流轉規則**：見 [狀態機定義](#五狀態機定義)
4. **取消限制**：ASSIGNED 狀態的訂單不可取消

---

## 三、領域模型定義

### 3.1 使用者與權限模型（UPMS）

#### 3.1.1 UpmsUser（使用者）

```java
- uuid: UUID (主鍵)
- username: String (唯一，帳號)
- password: String (BCrypt 加密後的密碼)
- enabled: Boolean (是否啟用)
- locked: Boolean (是否鎖定)
- lastLoginTime: LocalDateTime (最後登入時間)
- failedLoginAttempts: Integer (登入失敗次數)
- userRoles: Set<UpmsUserRole> (使用者角色關聯，一對多)
- profile: UpmsUserProfile (使用者個人資料，一對一)
```

**領域行為（Domain Methods）：**
- `changeUsername(String newUsername)`：變更使用者名稱（需檢查唯一性）
- `markLoginSuccess(LocalDateTime now)`：標記登入成功
- `lock(LocalDateTime now)`：鎖定帳號
- `unlock()`：解除鎖定
- `changePassword(String encodedPassword, LocalDateTime now)`：變更密碼
- `addUserRole(UpmsUserRole userRole)`：新增角色
- `removeUserRole(UpmsUserRole userRole)`：移除角色
- `replaceUserRoles(Collection<UpmsUserRole> newRoles)`：替換所有角色

#### 3.1.2 UpmsRole（角色）

```java
- uuid: UUID (主鍵)
- code: String (唯一，角色代碼，例如：ADMIN、DISPATCH)
- name: String (角色名稱)
- description: String (角色說明)
- enabled: Boolean (是否啟用)
- sortOrder: Integer (排序順序)
```

**領域行為：**
- `changeName(String newName)`：變更角色名稱
- `changeDescription(String newDesc)`：變更說明
- `enable()`：啟用角色
- `disable()`：停用角色
- `changeSortOrder(Integer sortOrder)`：變更排序

#### 3.1.3 UpmsPermission（權限）

```java
- uuid: UUID (主鍵)
- code: String (唯一，權限代碼，格式：{SYSTEM}_{RESOURCE}_{ACTION})
- systemCode: String (系統代碼，例如：UPMS、FMS、TOM)
- resourceCode: String (資源代碼，例如：USER、VEHICLE、ORDER)
- actionCode: String (操作代碼，例如：READ、CREATE、UPDATE、DELETE)
- name: String (權限名稱)
- description: String (權限說明)
- enabled: Boolean (是否啟用)
- sortOrder: Integer (排序順序)
- groupKey: String (群組鍵，例如：UPMS_USER)
```

**權限代碼命名範例：**
- `UPMS_USER_READ`：UPMS 模組的使用者讀取權限
- `FMS_VEHICLE_CREATE`：FMS 模組的車輛建立權限
- `TOM_ORDER_UPDATE`：TOM 模組的訂單更新權限

---

### 3.2 車隊管理模型（FMS）

#### 3.2.1 Vehicle（車輛）

```java
- id: UUID (主鍵，BINARY(16))
- plateNo: String (車牌號碼，唯一)
- type: VehicleType (車輛類型，Enum)
- status: VehicleStatus (車輛狀態，Enum，預設：AVAILABLE)
- brand: String (車輛品牌)
- model: String (車型)
- capacityTon: Double (載重噸數)
- currentDriverId: UUID (當前司機 UUID，可選)
- createdTime: LocalDateTime (建立時間，繼承自 BaseEntity)
- updatedTime: LocalDateTime (更新時間，繼承自 BaseEntity)
```

#### 3.2.2 Driver（司機）

```java
- id: UUID (主鍵，BINARY(16))
- name: String (姓名)
- phone: String (電話，唯一)
- licenseType: DriverLicenseType (駕照類型，Enum)
- status: DriverStatus (司機狀態，Enum，預設：ACTIVE)
- onDuty: Boolean (是否上線中，預設：false)
- currentVehicleId: UUID (當前綁定車輛 UUID，可選)
- userId: UUID (關聯 UpmsUser，可選)
- createdTime: LocalDateTime (繼承自 BaseEntity)
- updatedTime: LocalDateTime (繼承自 BaseEntity)
```

#### 3.2.3 DispatchTask（派工任務）

```java
- id: UUID (主鍵)
- orderId: UUID (關聯訂單)
- vehicleId: UUID (指派車輛)
- driverId: UUID (指派司機)
- status: DispatchStatus (派工狀態，Enum)
- assignedAt: LocalDateTime (指派時間)
- completedAt: LocalDateTime (完成時間，可選)
```

---

### 3.3 訂單模型

#### 3.3.1 OrderCore（訂單核心）

```java
- orderUuid: UUID (主鍵)
- orderNo: String (訂單編號，唯一)
- status: OrderCoreStatus (訂單高階狀態，Enum，預設：OPEN)
- createdTime: LocalDateTime (建立時間)
- updatedTime: LocalDateTime (更新時間)
```

**領域行為：**
- `create(String orderNo)`：建立訂單核心（Factory Method）
- `cancel()`：取消訂單（狀態 → CANCELLED）
- `close()`：結案訂單（狀態 → CLOSED）

#### 3.3.2 TomOrder（運輸訂單）

```java
- orderUuid: UUID (主鍵，與 OrderCore 共享)
- orderNo: String (訂單編號，與 OrderCore 同步)
- orderType: TomOrderType (訂單類型，Enum)
- tomStatus: TomOrderStatus (TOM 流程狀態，Enum，預設：NEW)
- customerUuid: UUID (客戶 UUID)
- customerName: String (客戶名稱，快照)
- pickupAddress: String (取貨地址)
- deliveryAddress: String (送貨地址)
- scheduledAt: ZonedDateTime (預定時間)
- customerRefNo: String (客戶參考單號)
- remark: String (備註)
```

**領域行為：**
- `create(...)`：建立運輸訂單（Factory Method）
- `rehydrate(...)`：從資料庫還原（Factory Method）
- `accept()`：受理訂單（狀態：NEW → ACCEPTED）
- `markAssigned()`：標記已指派（狀態：ACCEPTED → ASSIGNED）
- `ensureCanCancel()`：檢查是否可取消（ASSIGNED 不可取消）
- `ensureActive(boolean coreCancelled)`：檢查訂單是否仍有效

---

## 四、業務流程定義

### 4.1 使用者登入流程

```
1. 使用者輸入 username 與 password
2. AuthService 驗證使用者帳號與密碼
   - 檢查使用者是否存在
   - 檢查帳號是否啟用（enabled = true）
   - 檢查帳號是否鎖定（locked = false）
   - 使用 BCrypt 驗證密碼
3. 載入使用者權限（透過 UserRole → Role → RolePermission → Permission）
4. 產生 JWT Token（包含 username、authorities）
5. 更新使用者 lastLoginTime
6. 回傳 JWT Token 給前端
```

### 4.2 建立運輸訂單流程

```
1. 前端呼叫 POST /api/tom/orders（CreateTomOrderReq）
2. CreateTomOrderService.execute(CreateTomOrderCommand)
   a. 產生訂單編號（OrderNoGeneratorPort.nextTomOrderNo()）
     - 格式：TOM-YYYYMMDD-XXXX（可自訂）
     - 需處理編號衝突（重試機制，最多 3 次）
   b. 建立 OrderCore（CreateOrderCoreUseCase.execute）
     - OrderCore.create(orderNo)
     - status = OPEN
   c. 建立 TomOrder（TomOrder.create）
     - tomStatus = NEW
     - 驗證必要欄位（customerUuid、pickupAddress、deliveryAddress 等）
   d. 儲存 TomOrder（TomOrderRepository.save）
   e. 回傳 TomOrderResult
3. 回傳 ApiResult<TomOrderResp>
```

### 4.3 受理訂單流程

```
1. 前端呼叫 PUT /api/tom/orders/{id}/accept
2. ManageTomOrderUseCase.accept(orderUuid)
   a. 載入 TomOrder（TomOrderRepository.findByOrderUuid）
   b. 檢查 OrderCore 狀態（必須為 OPEN）
   c. 呼叫 TomOrder.accept()（Domain Method）
     - 檢查 tomStatus 必須為 NEW
     - 狀態轉換：NEW → ACCEPTED
   d. 儲存 TomOrder
3. 回傳 ApiResult<Void>
```

### 4.4 指派訂單流程（規劃中）

```
1. 前端呼叫 PUT /api/tom/orders/{id}/assign（AssignOrderReq：vehicleId, driverId）
2. ManageTomOrderUseCase.assign(orderUuid, vehicleId, driverId)
   a. 載入 TomOrder（狀態必須為 ACCEPTED）
   b. 檢查 OrderCore 狀態（必須為 OPEN）
   c. 檢查車輛狀態（Vehicle.status 必須為 AVAILABLE）
   d. 檢查司機狀態（Driver.status 必須為 ACTIVE 且 onDuty = true）
   e. 建立 DispatchTask（DispatchService.assign）
   f. 更新車輛狀態（Vehicle.status = BUSY）
   g. 更新司機 currentVehicleId
   h. 呼叫 TomOrder.markAssigned()（狀態：ACCEPTED → ASSIGNED）
   i. 儲存所有變更
3. 回傳 ApiResult<DispatchTaskResp>
```

### 4.5 建立車輛流程

```
1. 前端呼叫 POST /api/fms/vehicles（VehicleCreateReq）
2. VehicleService.create(VehicleCreateReq)
   a. 檢查車牌唯一性（VehicleRepository.findByPlateNo）
     - 若已存在，拋出 BusinessException
   b. 建立 Vehicle Entity（XkBeanUtils.copyProperties）
   c. 設定預設值（status = AVAILABLE）
   d. 儲存 Vehicle（VehicleRepository.save）
   e. 轉換為 VehicleResp（XkBeanUtils.copyProperties）
3. 回傳 ApiResult<VehicleResp>
```

### 4.6 建立司機流程

```
1. 前端呼叫 POST /api/fms/drivers（DriverCreateReq）
2. DriverService.create(DriverCreateReq)
   a. 檢查電話唯一性（DriverRepository.findByPhone）
     - 若已存在，拋出 IllegalArgumentException（建議改為 BusinessException）
   b. 建立 Driver Entity
   c. 設定預設值（status = ACTIVE、onDuty = false）
   d. 儲存 Driver
   e. 轉換為 DriverResp
3. 回傳 ApiResult<DriverResp>
```

---

## 五、狀態機定義

### 5.1 OrderCoreStatus（訂單核心狀態）

```
┌─────┐
│ NEW │ (新建時，自動設為 OPEN)
└───┬─┘
    │
    ▼
┌─────┐
│ OPEN│ ◄─────┐
└───┬─┘       │
    │         │ (所有 Domain 流程進行中)
    │         │
    ├─────────┘
    │
    ├── cancel() ───► ┌──────────┐
    │                 │CANCELLED │ (終態)
    │                 └──────────┘
    │
    └── close() ────► ┌────────┐
                      │ CLOSED │ (終態)
                      └────────┘
```

**狀態說明：**
- **OPEN**：訂單仍在進行中，允許所有 Domain 流程操作
- **CANCELLED**：訂單已被取消，不可再進行任何操作（終態）
- **CLOSED**：訂單已完成並結案，不可再進行任何操作（終態）

**狀態轉換規則：**
- 新建訂單：自動設為 OPEN
- OPEN → CANCELLED：呼叫 `OrderCore.cancel()`
- OPEN → CLOSED：呼叫 `OrderCore.close()`
- CANCELLED / CLOSED：不可再轉換（終態）

---

### 5.2 TomOrderStatus（運輸訂單流程狀態）

```
┌─────┐
│ NEW │ (新建訂單)
└───┬─┘
    │ accept()
    ▼
┌─────────┐
│ACCEPTED │ (已受理，可安排)
└───┬─────┘
    │ markAssigned()
    ▼
┌─────────┐
│ASSIGNED │ (已指派車輛/司機)
└─────────┘
```

**狀態說明：**
- **NEW**：新建訂單，尚未受理
- **ACCEPTED**：已受理，進入可安排階段
- **ASSIGNED**：已完成車輛/司機指派

**狀態轉換規則：**
- 新建訂單：tomStatus = NEW
- NEW → ACCEPTED：呼叫 `TomOrder.accept()`（需檢查當前狀態為 NEW）
- ACCEPTED → ASSIGNED：呼叫 `TomOrder.markAssigned()`（需檢查當前狀態為 ACCEPTED）
- ASSIGNED 狀態的訂單不可取消（`TomOrder.ensureCanCancel()`）

**限制條件：**
- TomOrderStatus 僅在 OrderCore.status = OPEN 時有意義
- 當 OrderCore.status ≠ OPEN 時，TomOrderStatus 僅作為歷史記錄保留

---

### 5.3 VehicleStatus（車輛狀態）

```
┌──────────┐
│AVAILABLE │ (可接單，空車)
└────┬─────┘
     │
     ├── assignTask() ───► ┌──────┐
     │                     │ BUSY │ (執行中)
     │                     └───┬──┘
     │                         │ completeTask()
     │                         │
     │◄────────────────────────┘
     │
     ├── startMaintenance() ──► ┌─────────────┐
     │                          │MAINTENANCE  │ (維修中)
     │                          └──────┬──────┘
     │                                 │ finishMaintenance()
     │                                 │
     │◄────────────────────────────────┘
     │
     └── scrap() ──────────────► ┌──────────┐
                                  │ SCRAPPED │ (報廢，終態)
                                  └──────────┘
```

**狀態說明：**
- **AVAILABLE**：可接單，未執行任何任務（空車）
- **BUSY**：執行中，有指派的任務
- **MAINTENANCE**：維修中，不可指派
- **RESERVED**：已預約，未開始執行（例如明天有排程）
- **SCRAPPED**：報廢 / 永久停用（終態）
- **INACTIVE**：停用（可選狀態）

**狀態轉換規則：**
- 新建車輛：status = AVAILABLE
- AVAILABLE → BUSY：指派任務時
- BUSY → AVAILABLE：任務完成時
- AVAILABLE → MAINTENANCE：進入維修
- MAINTENANCE → AVAILABLE：維修完成
- 任意狀態 → SCRAPPED：報廢（終態）

---

### 5.4 DriverStatus（司機狀態）

```
┌────────┐
│ ACTIVE │ (在職可派)
└────┬───┘
     │
     └── deactivate() ───► ┌──────────┐
                            │ INACTIVE │ (停用/離職，終態)
                            └──────────┘
```

**狀態說明：**
- **ACTIVE**：在職可派
- **INACTIVE**：停用/離職（終態）

**上線狀態（onDuty）：**
- `onDuty = true`：司機上線，可接任務
- `onDuty = false`：司機離線，不可接任務

**派工規則：**
- 只有 `status = ACTIVE` 且 `onDuty = true` 的司機才能被指派任務

---

### 5.5 DispatchStatus（派工任務狀態）

```
┌─────────┐
│ CREATED │ (建立但未派出)
└────┬────┘
     │ assign()
     ▼
┌─────────┐
│ASSIGNED │ (已指派，等待司機處理)
└────┬────┘
     │ driverStart()
     ▼
┌───────────┐
│IN_PROGRESS│ (司機已出發)
└────┬──────┘
     │ driverSign()
     ▼
┌────────┐
│ SIGNED │ (司機已完成簽收)
└────┬───┘
     │ confirm()
     ▼
┌──────────┐
│COMPLETED │ (後台確認無誤，結案)
└──────────┘

或

┌─────────┐
│ CREATED │
└────┬────┘
     │ cancel()
     ▼
┌──────────┐
│CANCELLED │ (派工取消)
└──────────┘
```

**狀態說明：**
- **CREATED**：建立但未派出
- **ASSIGNED**：已指派（等待司機處理）
- **IN_PROGRESS**：司機已出發
- **SIGNED**：司機已完成簽收（照片上傳完成）
- **COMPLETED**：後台確認無誤 → 結案
- **CANCELLED**：派工取消

---

## 六、業務規則

### 6.1 唯一性規則

| 實體 | 欄位 | 規則 | 驗證時機 |
|------|------|------|----------|
| UpmsUser | username | 必須唯一 | 建立、更新時 |
| UpmsRole | code | 必須唯一 | 建立、更新時 |
| UpmsPermission | code | 必須唯一 | 建立、更新時 |
| Vehicle | plateNo | 必須唯一 | 建立、更新時 |
| Driver | phone | 必須唯一 | 建立、更新時 |
| OrderCore | orderNo | 必須唯一 | 建立時（需處理衝突） |
| AdmDictCategory | code | 必須唯一 | 建立、更新時 |
| AdmDictItem | code | 同一分類下必須唯一 | 建立、更新時 |
| SysParam | key | 必須唯一 | 建立、更新時 |

### 6.2 狀態轉換規則

#### 6.2.1 訂單狀態轉換

- **OrderCore 取消限制**：任意狀態（OPEN）可轉換為 CANCELLED
- **OrderCore 結案限制**：只有 OPEN 狀態可轉換為 CLOSED
- **TomOrder 受理限制**：只有 NEW 狀態可轉換為 ACCEPTED
- **TomOrder 指派限制**：只有 ACCEPTED 狀態可轉換為 ASSIGNED
- **TomOrder 取消限制**：ASSIGNED 狀態不可取消

#### 6.2.2 車輛狀態轉換

- **指派任務限制**：只有 AVAILABLE 狀態的車輛可被指派
- **進入維修限制**：只有 AVAILABLE 或 BUSY 狀態的車輛可進入維修（BUSY 需先完成任務）

#### 6.2.3 司機派工限制

- **派工條件**：`status = ACTIVE` 且 `onDuty = true`
- **車輛綁定**：派工時自動綁定車輛（driver.currentVehicleId = vehicleId）

### 6.3 業務驗證規則

#### 6.3.1 訂單建立驗證

- **必要欄位**：customerUuid、customerName、pickupAddress、deliveryAddress、orderType
- **地址驗證**：pickupAddress 與 deliveryAddress 不可為空
- **時間驗證**：scheduledAt 必須為未來時間（可選，目前未實作）

#### 6.3.2 車輛建立驗證

- **車牌格式驗證**：plateNo 需符合台灣車牌格式（可選，目前未實作）
- **載重驗證**：capacityTon 必須 > 0（可選）

#### 6.3.3 司機建立驗證

- **電話格式驗證**：phone 需符合台灣手機格式（09XXXXXXXX，可選，目前未實作）
- **駕照類型驗證**：licenseType 必須為有效值

### 6.4 權限規則

#### 6.4.1 權限代碼命名規則

格式：`{SYSTEM}_{RESOURCE}_{ACTION}`

範例：
- `UPMS_USER_READ`：UPMS 模組的使用者讀取權限
- `FMS_VEHICLE_CREATE`：FMS 模組的車輛建立權限
- `TOM_ORDER_UPDATE`：TOM 模組的訂單更新權限

#### 6.4.2 權限檢查規則

- 使用者擁有的權限 = 其所有角色的權限集合（去重）
- API 端點可透過 `@PreAuthorize("hasAuthority('FMS_VEHICLE_CREATE')")` 進行權限檢查（目前未全面實作）

---

## 七、資料字典

### 7.1 車輛類型（VehicleType）

| 代碼 | 名稱 | 說明 |
|------|------|------|
| TRACTOR | 牽引車頭 | 車頭（牽引車） |
| TRAILER_FLATBED_20 | 20 尺貨櫃板車 | 20 尺板車 / 拖板 |
| TRAILER_FLATBED_40 | 40 尺貨櫃板車 | 40 尺板車 / 拖板 |
| TRUCK_SMALL | 小貨車 | 三噸半等 |
| TRUCK_MEDIUM | 一體式貨車 | 中型、重型貨車 |
| VAN | 廂型貨車 | 黑貓、宅配車常用 |
| REEFER | 冷凍車 | 保溫 / 冷凍配送 |
| OTHER | 其他 | 未分類車型 |

### 7.2 駕照類型（DriverLicenseType）

| 代碼 | 名稱 | 說明 |
|------|------|------|
| SMALL | 小型車 | 小型車駕照 |
| LARGE | 大型車 | 大型車駕照 |
| TRACTOR | 曳引車 | 曳引車駕照 |
| TRAILER | 拖車 | 拖車駕照 |

### 7.3 訂單類型（TomOrderType）

（需確認實際定義，目前程式碼中未看到完整定義）

### 7.4 角色代碼（UpmsRole）

| 代碼 | 名稱 | 說明 |
|------|------|------|
| ADMIN | 系統管理員 | 系統最高權限 |
| DISPATCH | 調度員 | 訂單調度與派工 |

（其他角色可透過 ADM 或系統設定新增）

---

## 八、API 端點摘要

### 8.1 UPMS 模組

- `POST /api/auth/login`：登入取得 JWT Token
- `GET /api/upms/users`：查詢使用者列表（分頁）
- `POST /api/upms/users`：建立使用者
- `PUT /api/upms/users/{id}`：更新使用者
- `PUT /api/upms/users/{id}/enabled`：啟用/停用使用者
- `GET /api/upms/roles`：查詢角色列表
- `POST /api/upms/roles`：建立角色
- `GET /api/upms/permissions`：查詢權限列表
- `POST /api/upms/permissions`：建立權限
- `PUT /api/upms/users/{id}/roles`：指派使用者角色
- `PUT /api/upms/roles/{id}/permissions`：指派角色權限

### 8.2 ADM 模組

- `GET /api/adm/dict/categories`：查詢字典分類列表
- `POST /api/adm/dict/categories`：建立字典分類
- `GET /api/adm/dict/categories/{id}/items`：查詢字典項目列表
- `POST /api/adm/dict/items`：建立字典項目
- `PUT /api/adm/dict/items/sort`：更新字典項目排序
- `GET /api/adm/sys-params`：查詢系統參數列表
- `POST /api/adm/sys-params`：建立系統參數

### 8.3 FMS 模組

- `GET /api/fms/vehicles`：查詢車輛列表（分頁）
- `POST /api/fms/vehicles`：建立車輛
- `PUT /api/fms/vehicles/{id}`：更新車輛
- `PUT /api/fms/vehicles/{id}/status`：更新車輛狀態
- `GET /api/fms/drivers`：查詢司機列表（分頁）
- `POST /api/fms/drivers`：建立司機
- `PUT /api/fms/drivers/{id}`：更新司機
- `PUT /api/fms/drivers/{id}/status`：更新司機狀態
- `PUT /api/fms/drivers/{id}/on-duty`：更新司機上線狀態
- `POST /api/fms/dispatch/assign`：指派派工任務

### 8.4 TOM 模組

- `GET /api/tom/orders`：查詢運輸訂單列表（分頁）
- `POST /api/tom/orders`：建立運輸訂單
- `GET /api/tom/orders/{id}`：查詢訂單詳情
- `PUT /api/tom/orders/{id}/accept`：受理訂單（規劃中）
- `PUT /api/tom/orders/{id}/assign`：指派訂單（規劃中）
- `PUT /api/tom/orders/{id}/cancel`：取消訂單（規劃中）

---

## 九、未來擴充規劃

### 9.1 訂單流程擴充

- 實作完整的訂單狀態機（包含 IN_TRANSIT、DELIVERED 等狀態）
- 實作訂單取消流程（包含取消原因、退款處理）
- 實作訂單結案流程（包含確認簽收、發票開立）

### 9.2 派工流程擴充

- 實作司機 App 端點（接收派工、更新狀態、上傳簽收照片）
- 實作派工任務的完整生命週期管理
- 實作車輛與司機的自動匹配邏輯

### 9.3 車輛管理擴充

- 實作車輛維修記錄的完整 CRUD
- 實作車輛里程記錄的完整 CRUD
- 實作車輛保險與證照到期提醒

### 9.4 報表與統計

- 實作訂單統計報表（日報、月報、年報）
- 實作車輛使用率統計
- 實作司機工作量統計

---

**文件維護：** 建議每次業務邏輯變更時更新此文件，確保文件與程式碼一致。

**最後更新：** 2026-01-10
